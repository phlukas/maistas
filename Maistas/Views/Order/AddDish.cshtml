@model IEnumerable<OrderedDish>
@{
	ViewData["title"] = "Pridėti patiekalai";
}

<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center p-2">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("AddOrderedDish")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Pridėti patiekalą</a>
	</span>
</div>


<div class="main-content-header container-fluid d-flex d-row justify-content-between align-items-center p-2">
    <span class="title">
        <h1>@ViewData["title"]</h1>
    </span>
</div>